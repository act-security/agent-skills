{
  "skill_name": "iam-tools",
  "evals": [
    {
      "id": 1,
      "prompt": "what does s3:Get*Tagging actually include?",
      "expected_output": "Runs iam-expand through npx and lists exactly the five s3 Get*Tagging actions verbatim, one per line, plus one sentence of interpretation.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Here is a policy in {{FILES}}/readonly-policy.json. List every action it grants, I need the full list for a review.",
      "expected_output": "Runs iam-expand on the policy JSON and returns every concrete action, either as the expanded JSON document or as a complete list, and says how many there are.",
      "files": ["readonly-policy.json"]
    },
    {
      "id": 3,
      "prompt": "is s3:GetObjekt a real IAM action?",
      "expected_output": "Runs iam-expand with --invalid-action-behavior error or include so the tool itself names the unknown action, and reports that s3:GetObjekt does not exist, rather than inferring it from an empty result.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "this action list in {{FILES}}/actions.txt is too long for my IAM policy, can you make it smaller without granting anything extra?",
      "expected_output": "Runs iam-shrink, then verifies with the expand round-trip and states that the diff is empty, before presenting the shrunk patterns verbatim.",
      "files": ["actions.txt"]
    },
    {
      "id": 5,
      "prompt": "shrink these actions: s3:GetObject, s3:PutObject, *",
      "expected_output": "Notices the bare * and explains the list already grants everything, so there is nothing to shrink, instead of returning a narrower-looking list.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "convert {{FILES}}/readonly-policy.json to terraform",
      "expected_output": "Runs iam-convert with --format tf and returns an aws_iam_policy_document data source in an hcl fenced block, unchanged from the tool output.",
      "files": ["readonly-policy.json"]
    },
    {
      "id": 7,
      "prompt": "I have this in {{FILES}}/wrapped-response.json from the AWS CLI, turn it into CDK python",
      "expected_output": "Unwraps the Document object before converting (or explains the 'Invalid policy provided' finding and unwraps), then returns the cdk-py output.",
      "files": ["wrapped-response.json"]
    },
    {
      "id": 8,
      "prompt": "when does the SCP in {{FILES}}/scp-region.json block us? show me a table",
      "expected_output": "Runs iam-truth with --policy-type scp --output md, shows the Markdown table verbatim, and explains that Not Denied means the SCP does not block the request, not that it is allowed.",
      "files": ["scp-region.json"]
    },
    {
      "id": 9,
      "prompt": "build a truth table for this identity policy: {{FILES}}/readonly-policy.json",
      "expected_output": "Says iam-truth supports SCPs and RCPs only and does not present an identity policy as if it were an SCP.",
      "files": ["readonly-policy.json"]
    },
    {
      "id": 10,
      "prompt": "can the role arn:aws:iam::123456789012:role/App read s3://prod-data in my account?",
      "expected_output": "Does not guess. Explains this needs the account's real policies and points to iam-lens with iam-collect, since none of the four tools evaluates a request against account data.",
      "files": []
    }
  ]
}
